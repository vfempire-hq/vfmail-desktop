source "https://rubygems.org"

# Fastlane is used ONLY on macOS CI runners to fetch signing certificates
# from the private vf-certificates repo via Match. See fastlane/Fastfile.
gem "fastlane", ">= 2.223"
